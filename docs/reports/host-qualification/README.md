# Standalone host qualification evidence

The M2 and M3 directories retain raw browser and native PTY samples, private-disk identity and browser screenshots. The visible directories retain all per-key WindowServer observations, provenance, complete-frame metadata, audit results and a contact sheet of every reference cursor row. Each visible run contains one cold edit and 30 warm samples.

The complete captured PNG sequences, private disks and probe sources are also exported in the task deliverable `editor-engine-milestone3-host-evidence.tar.xz`. The repository retains these smaller review records; repeatable probes and the audit live under `tools/`. The [host report](../editor-engine-host-qualification.md) defines the boundaries and reproduction steps. The frame observation is not physical monitor scanout.
